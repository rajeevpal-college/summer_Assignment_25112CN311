// Q103 Write a program to Create ATM simulation.

#include<bits/stdc++.h>
using namespace std;
int main(){
    int lang,card,inp,bal,amt,tra;
    bal=1000;
    cout<<"enter your atm card/account number";
    cin>> card ;
    cout<<"select  language 1 for hindi ,2 for english ";
    cin>>lang;
    cout<<"option 1 balance print";
    cout<<"option 2 add balance";
    cout<<"option 3 do transation";
    cin>>inp;

    if(inp==1){cout<<"available balance is" <<bal<<endl;

    }
    else if (inp==2){cout<<"enter amount/cash";
        cin>>amt;
        bal+=amt;
        
    }
    else if (inp==3){
        cout<<"enter amount";
        cin >> tra;
        if(tra<=bal){
            cout<<"tranaction successfull";
            cout<<"available balance"<<bal-tra<<endl;
            bal-=tra;}
            else {cout<<"insufficeinte balance";}

    }
    else{cout<<"prpgram ended";
        return 0;}
}
